syntax = "proto3";
option go_package = "github.com/brotherlogic/busybar-bridge/pkg/pb/busybar;busybar";

package BSB_Util;

enum Compression {
    PLAIN = 0;
    GZIP = 1;
}

message Json {
    Compression compression = 1;
    bytes data = 2;
}
